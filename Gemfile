source 'https://rubygems.org'
gem 'sinatra'
gem 'thin'

gem 'json'
gem 'data_mapper'

group :development do
  # YOLO-style database
  gem 'dm-sqlite-adapter'
end

group :production do
  # Actual database
  gem 'dm-postgres-adapter'
end
